import * from "std/env"
import * from "../utils.ab"

pub fun print_confirm_help(): Null {
    const usage = ["Usage:", "./xylitol.sh", "confirm", "[flags]"]
    print_wrapped(usage)
    echo("")
    const title = [colored_primary("confirm"), "-", "Display", "a", "Yes/No", "confirmation", "dialog."]
    print_wrapped(title)
    echo("")
    printf(colored_secondary("Flags:") + "\n")
    const names = [
        "-h, --help",
        "--header=\"<text>\"",
        "--default=<yes|no>",
    ]
    const texts = [
        "Show this help message",
        "Set a header text to display above the options",
        "Set the default selection",
    ]
    const notes = [
        "",
        "(ANSI escape supported)",
        "(default: yes)",
    ]
    render_help_entries(names, texts, notes)
    echo("")
}
