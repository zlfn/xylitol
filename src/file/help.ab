import * from "std/env"
import * from "../utils.ab"

pub fun print_file_help(): Null {
    const usage = ["Usage:", "./xylitol.sh", "file", "[<path>]", "[flags]"]
    print_wrapped(usage)
    echo("")
    const title = [colored_primary("file"), "-", "Browse", "filesystem", "and", "select", "a", "file."]
    print_wrapped(title)
    echo("")
    printf(colored_secondary("Arguments:") + "\n")
    const arg_names = ["[<path>]"]
    const arg_texts = ["Starting directory path"]
    const arg_notes = ["(default: current directory)"]
    // 20 keeps this section on the same column as Flags below.
    render_help_entries(arg_names, arg_texts, arg_notes, 20)
    echo("")
    printf(colored_secondary("Flags:") + "\n")
    const names = [
        "-h, --help",
        "-a, --all",
        "--cursor=\"<text>\"",
        "--path=\"<path>\"",
        "--page-size=<number>",
    ]
    const texts = [
        "Show this help message",
        "Show hidden files",
        "Set the cursor text",
        "Set the starting directory path",
        "Set the number of entries per page",
    ]
    const notes = [
        "",
        "",
        "(default: '> ')",
        "",
        "(default: 10)",
    ]
    render_help_entries(names, texts, notes)
    echo("")
}
